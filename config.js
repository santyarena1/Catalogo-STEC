/* ============================================================
   CONFIG.JS — Configuración del sitio Crestron Home Argentina
   ============================================================ */

window.APP_CONFIG = {
  /* ── Serper (búsqueda de imágenes vía Google) ─────────────── */
  /* Configurar localmente — no incluir en el repo              */
  SERPER_KEY: '',

  /* ── OpenAI (generación de blog con IA) ──────────────────── */
  /* Configurar localmente — no incluir en el repo              */
  OPENAI_KEY: '',

  /* ── JSONBin.io (sincronización de anotaciones) ───────────── */
  /* 1. Crear cuenta en https://jsonbin.io                      */
  /* 2. New Bin → contenido inicial: []  → guardar el Bin ID    */
  /* 3. Account → API Keys → copiar Master Key                  */
  JSONBIN_ID:  '',
  JSONBIN_KEY: '',

  /* ── Credenciales de administrador ───────────────────────── */
  ADMIN_USER: 'admin',
  ADMIN_PASS: 'Stec2024!',
};
